<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Flask App{% endblock %}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.2/css/bulma.min.css" />
    <style>
        body {
            font-family: Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4; /* Light grey background for a clean look */
            color: #333;
            display: flex;
        }
        .sidebar {
            width: 200px;
            height: 100vh;
            background-color: maroon;
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 1rem;
            position: fixed;
            top: 0;
            left: 0;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
        }
        .sidebar .navbar-item {
            color: white;
            padding: 1rem;
            text-align: center;
            width: 100%;
            border-radius: 5px;
            transition: background-color 0.3s;
        }
        .sidebar .navbar-item:hover {
            background-color: #b22222; /* Darker maroon for hover effect */
        }
        .content {
            margin-left: 200px; /* Offset the main content from the sidebar */
            padding: 2rem;
            width: calc(100% - 200px); /* Ensure content takes the remaining width */
            background-color: white; /* White background for the content area */
            min-height: 100vh;
        }
        .button {
            background-color: maroon;
            color: white;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        .button:hover {
            background-color: #b22222;
        }
    </style>
</head>
<body>
    <div class="sidebar">
        <a href="{{ url_for('main.index') }}" class="navbar-item">Home</a>
        {% if current_user.is_authenticated %}
            <a href="{{ url_for('main.profile') }}" class="navbar-item">Profile</a>
            {% if current_user.is_admin %}
                <a href="{{ url_for('main.admin_profile', student_no='some_student_no') }}" class="navbar-item">Admin Profile</a>
            {% endif %}
            <a href="{{ url_for('auth.logout') }}" class="navbar-item">Logout</a>
        {% else %}
            <a href="{{ url_for('auth.login') }}" class="navbar-item">Login</a>
            <a href="{{ url_for('auth.signup') }}" class="navbar-item">Sign Up</a>
        {% endif %}
    </div>

    <div class="content">
        {% block content %}
        {% endblock %}
    </div>
</body>
</html>

